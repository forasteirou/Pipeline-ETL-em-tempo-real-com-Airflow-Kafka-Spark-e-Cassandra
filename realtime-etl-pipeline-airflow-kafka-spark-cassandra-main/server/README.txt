

# Open the terminal or command prompt, navigate to the folder with the files, and run:

docker compose up -d

# The command below can be used to shut down all containers:

docker-compose down

# If you make any changes to the YAML file, the command below can be used to rebuild only the necessary image:

docker-compose up --build -d
